你需要实现这个模型和他的训练系统。
1、模型整体采用Pre-Norm Transformer架构。
2、模型采用统一骨干同时处理多种异构数据。
3、模型接受历史10帧，5Hz的OverView RGB和Wrist RGB，Patch=16,16，采用ImageNet归一化；相对50Hz原始数据严格每10帧采一帧。
4、模型接受25Hz的接触力传感器，Patch=8,8，相对50Hz原始数据严格每2帧采一帧。完整触觉图按训练集统计进行三通道归一化，左右指共享同一组参数；未来双指触觉摘要的对应7维也共享归一化参数。触觉嵌入层一定要开偏置，不然很容易就输出0导致模型注意力崩盘。
5、模型接受25Hz的末端执行器位姿和运动学状态以及执行动作状态（注意归一化，你需要读取数据集后决定归一化系数），每帧编码为单个Token。
6、模型接受闭集语言描述任务。
7、模型序列末端拼接50个PredictToken（2s，25Hz），在归一化数据空间做流匹配预测，预测接触力摘要和各关节执行动作，初始化为高斯噪声，且不参与残差；每个Transformer层（SDPA+FFN）输出后采用全层共享速度场解码器预测归一化数据空间的速度场，然后前移速度后再次嵌入，嵌入全局共享。训练早期采用教师强制，逐渐Warm-Up将GT输入替换为模型预测输入。注意PredictToken不直接在层间传递。
10、模型需要预测当前所处任务阶段。
11、位置编码方面，由于序列的复杂性，采用可学习的位置编码，具体来说，有一个Linear->AdaLN->SiLU->Linear的层专门用于位置处理（AdaLN用于编码层，15个通道（Predictor也用它），对应开启）。这个层接受以下几类输入作为条件，输出一个384维的向量作用于每一层SDPA的QK部分（相加，且绝对不要进入主要特征流，也就是不要直接作用于V），然后QKNorm：模态身份（OverView RGB、Wrist RGB、接触力传感器、末端执行器、闭集语言描述符、CLSToken、RegisterToken、PredictToken这几种模态），时间（使用正余弦编码，便于模型能够精确感知时间；完整4秒窗口映射到[0,π/2]，不乘2π），OverView RGB和Wrist RGB使用PETR射线几何，每条射线采样32点：Overview在0.20--2.20m采用镜像对数采样，近疏远密；Wrist在0.02--1.20m采用普通对数采样，近密远疏。三维点线性归一化到[-1,1]，Wrist使用逐帧动态外参。接触力传感器和末端执行器需要编码实际坐标（接触力传感器需要逐Patch坐标并区分是左还是右，因此需要多一个通道区分，并且接触力传感器是动的），也需要归一化。历史观测使用窗口时间0--2秒，PredictToken使用未来窗口时间2--4秒并编码PredictToken模态；PredictToken不编码几何，流匹配噪声时间不作为位置时间。
12、为了便于FFN区分模态，每层采用模态独立的Lora（仅作用于FFN的第一层），秩=2，Lora支路残差采用一个线性层从每个Token预测一个标量权重，用于逐Token控制Lora控制力度，采用Sigmoid激活，初始化为输出0.5，Lora初始化为0。
13、对于注意力掩码，闭集语言需要因果掩码，其余模态对闭集语言单向可见，OverView RGB、Wrist RGB、接触力传感器、末端执行器等双向可见，PredictToken单向可见其余模态，自身双向可见。
14、模型采用384维特征维度、6头注意力、FFN采用第一层：384->3072，SwiGLU（能够将维度减半），第二层：1536->384，共12层Transformer层
15、训练方面采用覆盖全部固定传感器Token的掩码策略。75%的样本首先掩码任务相关视觉Patch，再按全序列连续时间梯度提高动态区域的掩码概率，任务相关优先级高于时间梯度；其余25%的样本完全随机。支持整段或局部掩码接触力与末端执行器状态；这些模态被整段掩码时降低RGB掩码力度，以保留足够上下文。全序列总掩码率保持约75%。骨干接受完整序列，但被掩码区域作为注意力Key不可见；Predictor（3层）采用MaskToken填充残缺区域并重建EMA教师完整特征，EMA系数为0.9995。
16、在残差连接方面，每层需要学习一系列Logits，经过Softmax后作为权重控制该层之前的所有层的输出加权作为该层输入，类似于一种密集连接，这可以保证特征流尺度理想状态下始终一致。注意PredictToken不参与残差连接。
17、模型所有的Norm采用RMSNorm。
18、数据方面，采用在线渲染策略，由于数据集文件储存了每一帧的完整状态，因此可以直接读取对应帧的状态进行渲染。
19、数据集中有一些失败恢复样本，不要用失败恢复样本的失败部分监督模型，但可以利用失败恢复的恢复部分监督（自然的，失败部分就作为输入了）。
20、注意张量形状的对齐，这里面部分操作可能会改变张量形状，因此需要padding或掩码。
21、Loss方面，对于被掩码区域权重1，无掩码区域权重0.5，对于PredictToken去噪，这个可能需要你权衡。
